node_modules/
dist/
build/
.DS_Store
*.log

# Environment files — never commit real credentials
.env
.env.local
.env.*.local

# Uploaded product photos — keep the folder, not the files
backend/uploads/*
!backend/uploads/.gitkeep
